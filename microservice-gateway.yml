server:
  port: 9004
spring:
  cloud:
    gateway:
      routes:
        - id: patients-service
          uri: lb://patients-service
          predicates:
            - Path=/patients/**
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8083/eureka/