spring:
  cloud:
    gateway:
    discovery:
      locator:
        enabled: true
    routes:
      - id: patients
        uri: lb://patients-service
        predicates:
        - Path=/patients/**
  security:
    user:
      name: newenpoi
      password: azerty
server:
  port: 9103
eureka:
  client:
    serviceURL:
      defaultZone: http://localhost:9102/eureka